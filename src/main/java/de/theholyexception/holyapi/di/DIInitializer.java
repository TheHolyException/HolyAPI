package de.theholyexception.holyapi.di;

/**
 * Callback when an instance is created via internal di logic
 */
public interface DIInitializer {
	/**
	 * Callback when an instance is created via internal di logic
	 */
	void initialize();
}
